# Este projeto usa configuração via LocalStorage (Client-Side).
# Nenhuma variável de ambiente de build é estritamente necessária.
# Se desejar forçar configurações padrão, você precisaria alterar o código para ler process.env.

# Exemplo (não utilizado atualmente no código):
# NEXT_PUBLIC_API_URL=https://...
